# Simulation
cocotb>=2.0            # sim/examples uses the 2.x API (unit= not units=)
pytest>=8.0

# Host-side analysis and GUI (M4 onward)
numpy>=1.26
scipy>=1.11
pyqtgraph>=0.13
PyQt6>=6.6

# Transport (M4) -- on Windows one of these, see docs/setup-windows.md
# pyftdi>=0.55         # needs libusbK via Zadig
# ftd2xx>=1.3          # uses FTDI's native Windows driver, no Zadig

# Serial, used for the UART readout in M2
pyserial>=3.5
